# Manuscript-to-Code Traceability Matrix

Evidence basis: `manuscript.tex` (2,406 lines, read in full); `source code/*.py` (13 files); `SecureLoan.ipynb` (56 cells — content-identical to `source code/*.py`); `Public_private_key_pairs/`; `Results/`; `image/`; `user information/`; `README.md`; `User Guide.md`; `HE_configuration.txt`; `database_configuration.txt`. No `requirements.txt`/environment file, no test directory, and no LendingClub CSV or notebook exist in the repo (`grep`/`find` confirm zero references to LendingClub, `pandas`, `sklearn`, `demographic`, `equalised`, `compSim`, `alpha1`, `winsor`, `LPU`, `FLA`, or `sk_HE` outside `manuscript.tex` itself).

| Manuscript claim | Section/Algorithm | Expected implementation | Actual file/function | Evidence | Status | Severity |
|---|---|---|---|---|---|---|
| Two-role separation: LPU (operational) vs. FLA (independent auditor) | §1 Contributions; §4 intro | Distinct modules/roles with different key material and message flows | None — repo has "Customer / Bank / CIBIL / Third_party" single-flat-script roles, no LPU or FLA concept anywhere | `grep -ri "LPU\|FLA" source*` → no hits | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED | CRITICAL |
| Gender never disclosed to LPU in plaintext; only CKKS ciphertext | Threat 1; §4.8 | LPU code path never calls `HE.Dec`/`.decrypt()` on gender | `source code/Loan_approval.py:5`: `check_gender = round(SoP_gender.decrypt().tolist()[0])` — the same script that makes the approval-relevant check calls `.decrypt()` directly on the gender-similarity ciphertext | `Loan_approval.py` lines 3–5 | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED (and inverted: the one "audit-like" role that exists decrypts, rather than only the never-implemented FLA) | CRITICAL |
| `sk_HE` retained exclusively by FLA; LPU never receives it | §4.2 Initialisation; Threat 1 | Two disjoint TenSEAL contexts, secret-key-bearing context never leaves FLA process/module | `Enter_details.py::context()` creates **one** `ts.context(...)` (a full, secret-key-bearing context; `make_context_public()` is never called) and that same context object/global is reused by `Loan_approval.py` to `.decrypt()` results. There is only one key custodian in the whole codebase, and it is the same code that evaluates the match. | `Enter_details.py` lines 18–24; `Loan_approval.py` lines 3–5 | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED | CRITICAL |
| Protected attribute encoded as **binary** one-hot g_i∈{(1,0),(0,1)} | §4.6; Table 1 (notation) | A length-2 CKKS-encrypted pair per applicant | `Enter_details.py` encodes gender as a length-**4** category vector `[Male,Female,Transgender,Other]` (and separately encrypts caste(6)/religion(8)/orientation(6)/ethnicity(10) vectors) — not the manuscript's 2-dim scheme, and not restricted to gender | `Enter_details.py` lines 36, 438 | IMPLEMENTED DIFFERENTLY | HIGH |
| Encrypted reference vectors HE.r_m=(1,0), HE.r_f=(0,1), generated once by FLA and distributed to LPU | Algorithm 1; §4.6 | Fixed reference ciphertexts created at init and reused across applicants | No reference vectors at all. Instead each run re-encrypts a hardcoded "recorded" answer (`gender = ts.plain_tensor([0,1,0,0], ...)`) that stands in for a specific individual's stored record, and compares it against that same individual's freshly entered answer — an identity/consistency check, not a group-reference match | `Enter_details.py` lines 38, 43 | NOT IMPLEMENTED | HIGH |
| `compSim`: 2 ciphertext×ciphertext mults + 1 addition, depth-1, relinearise+rescale, no normalisation | §4.6 | A function named/shaped like `compSim(HE.g_i, HE.r_k)` | `Loan_approval.py` performs `encrypted_gender * encrypted_input_gender` then `.sum(axis=0)` on 4/6/8/6/10-dim vectors — same high-level HE pattern (ct×ct mult + hom. sum) but wrong dimensionality, no named `compSim`, no reference-vector abstraction, and result is decrypted inline rather than fed into an aggregate | `Loan_approval.py` lines 3–39 | IMPLEMENTED DIFFERENTLY | HIGH |
| CKKS params: poly modulus degree 8192, coeff moduli [60,40,40,60], scale 2^40 | §4.6; §6.1.4 | `ts.context(ts.SCHEME_TYPE.CKKS, 8192, coeff_mod_bit_sizes=[60,40,40,60])`, `global_scale = 2**40` | Matches exactly | `Enter_details.py` lines 19–21 | FULLY IMPLEMENTED (parameters only — not the surrounding protocol) | INFO |
| Multiplicative depth of compSim = 1, no bootstrapping | §4.6 | Single mult+rescale per coordinate before one addition | `Loan_approval.py` does a single `*` then `.sum()` — depth-1 in the shape used, consistent with the claim, but on unrelated vectors (see above) so the depth claim is not verified in the context the manuscript actually describes (group-reference matching) | `Loan_approval.py` | CANNOT VERIFY (right shape, wrong purpose/vectors) | MEDIUM |
| Bank/CA/IP digital signatures: σ_b,i=Sign(uid‖acc_i), σ_c,i=Sign(uid‖s_i), σ_g,i=Sign(uid‖d_g,i) | §4.5.1; §6.1.4 ("digital signatures are implemented") | RSA-PSS or similar `Sign`/`VerifySig` calls tied to Bank/CA/IP private keys, verified by LPU | **No signature scheme exists anywhere in the source tree.** The only `.sign(...)` call in the whole repo is in `Decrypt_verification.py` line 36, which signs the contents of `loan_information.txt` using an undefined name `private_key` (never assigned in that file — a `NameError` at runtime) and is unrelated to Bank/CA/IP credentialing. `Key_generation.py` produces only RSA **encryption** key pairs (no separate signature key pairs for any party). | `Decrypt_verification.py` lines 33–43; `Key_generation.py` whole file | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED | CRITICAL |
| NIZKP relations R_acc, R_score, R_bind (Setup/Prove/Verify, formalised at protocol level only) | §4.5.1; §6.4 "Implementation boundary" | None expected in code — manuscript explicitly says these are not concretely implemented | `Account open.py::NIZKP()` implements an ad-hoc discrete-log "proof" over Name/Address/DoB/Phone that is unrelated to any of R_acc/R_score/R_bind (no Bank signature, no account commitment, no CA signature, no score commitment, no application-binding hash). Its own construction is additionally broken as a ZK protocol: it reuses **one** commitment `r·G` and **one** challenge `c` to "prove" four independent secrets, so the verification equation only holds if the four hashed values happen to be equal — it is not a valid Σ-protocol for four independent witnesses. It is also **not non-interactive** (no Fiat–Shamir transform: the "challenge" is generated locally by the same process, immediately after the commitment, with no hash-based binding to the commitment or the statement). | `Account open.py` lines 1–53 | NOT IMPLEMENTED AND PAPER ALSO SAYS NOT IMPLEMENTED for the *manuscript's* R_acc/R_score/R_bind — but flagged separately because the repo's unrelated same-named `NIZKP()` function could mislead a reader into thinking the paper's NIZKPs are prototyped | INFO for the paper's own claim (correctly marked out-of-scope); HIGH as a documentation-hygiene issue (see Gap Report D) |
| Credit-score computed via `ComputeScore(employmentStatus, salary, creditHistory)` | Algorithm 3 | A scoring function taking the three inputs | `Apply_loan.py` line 79: `CIBIL_score = 550` — a literal constant, ignoring all decrypted inputs | `Apply_loan.py` line 79 | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED (score computation is asserted as part of the operational prototype) | HIGH |
| Loan decision Ŷ_i = I[s_i ≥ τ] | Algorithm 5; Table 1 | An explicit threshold comparison against a public τ | No `τ`, no comparison of the score against any threshold anywhere in the repo. The only "decision" logic (`Approval_decision.py`) branches on `check_gender==1 or check_casteCat==0` — i.e., decides scheme eligibility from the **protected-attribute match results**, not from the credit score | `Approval_decision.py` lines 11–14 | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED (and the one decision rule that exists is the opposite of the stated design goal: it consumes decrypted protected-attribute match flags as a condition) | CRITICAL |
| Rejected applications remain in C_k (fairness-audit denominator) | §4.7 discussion; Security Goal 2 | An accumulation loop over a batch of applications, threshold applied only to Â_k not to C_k | No batch/audit-period loop of any kind exists — the code processes one applicant end-to-end per run and prints intermediate values; there is no `C_k`, no persistence of rejected-vs-approved counts | — | CANNOT VERIFY (construct does not exist to check) | CRITICAL (as an absence, not as an observed defect) |
| Encrypted aggregate sufficient statistics S={C_k,A_k,P_k,TP_k,N_k,FP_k} sent LPU→FLA | Algorithm 5 | 12 running HE ciphertexts, transmitted once per audit batch | Not present | — | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED | CRITICAL |
| FLA: decrypt 12 aggregates, min-cell-size suppression k_min, Δ_DP, Δ_EO formulas | Algorithm 6; §4.8 | A `k_min` check and the two fairness formulas | Not present anywhere in the repository | — | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED | CRITICAL |
| LendingClub dataset ingestion, ~890k records, 70/10/20 stratified split | §6.1.1 | Data-loading/preprocessing script | No CSV, no data-loading code, no `pandas`/`sklearn` import anywhere in repo | `grep -rl "pandas\|sklearn"` → empty | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED (evaluation section implies a completed pipeline exists, though several nearby disclaimers say specific numbers are not reported — the pipeline's *existence* as code is still asserted, e.g. "we obtain approximately 890,000 observations") | HIGH |
| Synthetic protected attribute Pr(g=female\|x)=σ(α0+α1·z), Bernoulli sampling, 5 named proxy features, winsorisation, region mapping | §6.1.1 | A label-generation function | Not present | — | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED (as a described, parameterised procedure — though the manuscript itself, more carefully, only ever reports the *sweep* over α1/seeds as not-yet-run; the base single-setting generator's code existence is implied but unverifiable) | MEDIUM |
| Phase-level computation timings, Fig. 3 (e.g. account-opening 8.887 ms→571.9 ms at batch 1→100) | §6.4.1 | Benchmark harness varying batch size 1–100 and timing each phase | No benchmark script, no timing code (`time.time()`/`timeit`), no batching concept, no Figures 3/4 source data in repo | — | CANNOT VERIFY / TRACKED BUT NOT REPRODUCIBLE (no script, no raw data, no plot-generation code checked in — only PNGs would be referenced, and even those specific filenames (`Computation-cost.png`, `communication-cost.png`) are **not present** under `image/` or `Results/`, which instead contain unrelated screenshots of the 2023 eligibility-check demo) | HIGH |
| Table 7 serialised object sizes (786,560 B protected-attribute ciphertext pair, 262,208 B aggregate ciphertext, 524,352 B pk_HE, 1,573,120 B evk_HE, 3,146,752 B aggregate packet) | §6.1.4; Table 7 | Either a serialization script or an explicit analytical derivation | No serialization-measurement code exists. The manuscript itself resolves this internally as "analytical estimates" (see Consistency Finding #2) — repository confirms there is no measurement code, consistent with "estimate," but the manuscript's *other* passage claiming these are "obtained from actual serialised objects" has no support at all | — | NO SUPPORT FOUND (for the "measured" framing); CANNOT VERIFY the arithmetic derivation independently without running TenSEAL, though the byte-math shown in §6.1.4 (2×8192×3×8=393,216 etc.) is internally arithmetically consistent | MEDIUM |
| Hash function H(·)=SHA-256 for digests/commitments | §6.1.4 | `hashlib.sha256` used for `d_g,i`, `C_app,i`, etc. | `hashlib.sha256` is used, but only for password hashing (`Sign_in.py`, `Login.py`) and PII/ID hashing (`Registration and ID Generation.py`) — never for a ciphertext digest or application-binding commitment as specified | `Sign_in.py` line 69; `Registration and ID Generation.py` lines 7, 24 | IMPLEMENTED DIFFERENTLY (primitive present, application absent) | LOW |
| "A plain hash transmitted together with a ciphertext is treated only as an implementation-level consistency check" (Account-credential issuance Algorithm 2, hash-then-check pattern) | §4.4, Algorithm 2 | `h_1i`/`h'_1i` compare pattern around encrypted PII transmission | Not present — `Account open.py` has no hash-based integrity check around its RSA-encrypted PII transmission | — | NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED | MEDIUM |
| SQLite persistent storage | §6.1.4 ("SQLite was used for persistent storage") | `sqlite3` usage for credential/application storage | `Sign_in.py`/`Login.py`/`display_user.py`/`Remove_user.py` do use `sqlite3`, but only for a trivial `users(username, password_hash)` table — no account/score/gender-credential storage | `Sign_in.py` lines 5–19 | PARTIALLY IMPLEMENTED | LOW |
| Petlib for elliptic-curve operations | §6.1.4 | `petlib.ec` usage | `Account open.py` and `Sign_in.py` import `petlib.ec.EcGroup`/`Bn` for the broken `NIZKP()` function only | `Account open.py` lines 6–7 | PARTIALLY IMPLEMENTED (library used; not for anything matching R_acc/R_score/R_bind) | LOW |
| TenSEAL for CKKS | §6.1.4 | `tenseal` usage | Confirmed | `Enter_details.py` line 15, `HE_configuration.txt` | FULLY IMPLEMENTED (library choice only) | INFO |
| Threat 5/6, security theorem, leakage-profile table (Table "leakage_profile") — LPU/FLA non-collusion, IND-CPA argument | §5 | N/A — this is a security *argument about* the design, not code | No corresponding code exists to evaluate (there is no LPU/FLA split to which the argument could even apply) | — | CANNOT VERIFY / NOT APPLICABLE (the security proof concerns an architecture the repo does not instantiate) | INFO |
| README/User Guide claims: "protects sensitive personal information," "employs homomorphic encryption and zero-knowledge proofs to enable bank officers to verify applicant eligibility without accessing confidential data" | README.md lines 9, 24, 28 | Should describe what the *repo* does | The repo's actual behaviour is that the officer-facing script (`Loan_approval.py`) **does** decrypt the protected-attribute match result in plaintext (`.decrypt()`), directly contradicting "without accessing confidential data" | `Loan_approval.py` lines 3–39; README.md line 24 | REPO CONTRADICTS README/DOCUMENTATION | CRITICAL (documentation/overclaim, compounding the code-level privacy-boundary defect above) |

### Summary counts

| Status | Approx. count of matrix rows |
|---|---|
| FULLY IMPLEMENTED | 2 (both are bare parameter/library choices, not protocol behaviour) |
| PARTIALLY IMPLEMENTED | 2 |
| IMPLEMENTED DIFFERENTLY | 4 |
| NOT IMPLEMENTED BUT PAPER SAYS IMPLEMENTED | 11 |
| NOT IMPLEMENTED AND PAPER ALSO SAYS NOT IMPLEMENTED | 1 (NIZKP concretisation, correctly disclaimed by the paper) |
| REPO IMPLEMENTS EXTRA FUNCTIONALITY | 1 (caste/religion/orientation/ethnicity encrypted matching — not claimed by the manuscript at all) |
| CANNOT VERIFY | 4 |

This distribution is one-sided by construction, not by selective reporting: the repository is a different, earlier project, so almost every manuscript-claimed prototype behaviour has no counterpart to find.
